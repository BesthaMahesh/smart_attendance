[build]
  builder = "NIXPACKS"

[phases.setup]
  nixPkgs = ["python311", "cmake", "gcc"]

[phases.install]
  cmds = ["pip install --only-binary=:all: -r requirements.txt || pip install -r requirements.txt"]
